<div>
<%= form_with(model:[@post, @comment], local: true, url: user_post_comments_path) do |form| %>
   <div>
    <%= form.label :text %><br/>
    <%= form.text_area :text, rows:3, placeholder: 'Enter your Comment' %>
  </div>
  <p>
    <%= form.submit 'Comment' %>
  </p>
<% end %>
</div>
